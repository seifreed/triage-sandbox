"""Domain layer: entities, rules and errors, with no dependency on any framework.

The public API of the library is curated once, in the package root; import
from `triage_sandbox` rather than from this package.
"""
